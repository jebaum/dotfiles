set from = "jebaum@ucla.edu"

# NOTE - do not end folder path with '/'
set spoolfile = +ucla/inbox   # controls what folder is in mutt on load
# set record = +jebaumg/sent
unset record  # avoid duplicates
set postponed = +ucla/drafts

set sendmail = "/usr/bin/msmtp -a jebaumg"
color status color51 default
# vim:filetype=muttrc
