set from = "jebaum@ucla.edu"

# NOTE - do not end folder path with '/'
set folder = ~/.mutt/mail/jebaumg       # specifies common base path
set spoolfile = +INBOX                  # controls what folder is in mutt on load
set record = +sent
set postponed = +drafts

set sendmail = "/usr/bin/msmtp -a jebaum"  # not jebaumg! use ucla's mail server

color status white black
# vim:filetype=muttrc
