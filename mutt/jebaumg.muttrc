set from = "jebaum@ucla.edu"

# NOTE - do not end folder path with '/'
set spoolfile = +jebaumg/INBOX                  # controls what folder is in mutt on load
set record = +jebaumg/sent
set postponed = +jebaumg/drafts

set sendmail = "/usr/bin/msmtp -a jebaum"  # not jebaumg! use ucla's mail server
color status color51 default
# vim:filetype=muttrc
