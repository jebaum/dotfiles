set from = "neurionb@gmail.com"

# NOTE - do not end folder path with '/'
set spoolfile = +neurionb/INBOX                  # controls what folder is in mutt on load
set record = +neurionb/sent
set postponed = +neurionb/drafts

set sendmail = "/usr/bin/msmtp -a neurionb"
color status color208 default
# vim:filetype=muttrc
