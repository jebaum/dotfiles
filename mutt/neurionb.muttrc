set from = "neurionb@gmail.com"

# NOTE - do not end folder path with '/'
set folder = ~/.mutt/mail/neurionb      # specifies common base path
set spoolfile = +INBOX                  # controls what folder is in mutt on load
set record = +sent
set postponed = +drafts

set sendmail = "/usr/bin/msmtp -a neurionb"

color status white black
# vim:filetype=muttrc
